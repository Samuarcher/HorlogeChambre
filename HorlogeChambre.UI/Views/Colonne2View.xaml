<UserControl x:Class="Samuarcher.HorlogeChambre.UI.Views.Colonne2View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:Samuarcher.HorlogeChambre.UI.ViewModels"
             mc:Ignorable="d" 
			 d:DataContext="{d:DesignInstance viewModels:Colonne2ViewModel}"
             d:DesignHeight="300" d:DesignWidth="300">
	<StackPanel Orientation="Vertical">
		<GroupBox Header="Météo">
			<Image Source="{Binding MeteoImagePath}" Width="200" Height="200"/>
		</GroupBox>
		<GroupBox Header="Lune">
			<Image Source="{Binding LuneImagePath}"/>
		</GroupBox>
	</StackPanel>
</UserControl>
